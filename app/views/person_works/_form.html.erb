<%= form_for(person_work) do |f| %>
  <% if person_work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(person_work.errors.count, "error") %> prohibited this person_work from being saved:</h2>

      <ul>
      <% person_work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %>
    <%= f.text_field :person_id %>
  </div>

  <div class="field">
    <%= f.label :work_id %>
    <%= f.text_field :work_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
